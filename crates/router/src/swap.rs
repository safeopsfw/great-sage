//! Hot-swap LoRA loader.
//!
//! Loads a LoRA adapter from disk, applies it to the base model,
//! and frees the previous adapter. Loading takes a fraction of a second.

// TODO: Phase 6 — implement LoRA hot-swap mechanism.
